/* ============================================================
   KGS CONSTRUCTION & TRANSPORT — slider.js
   Ticker and Slider Utilities
   ============================================================ */

function initSliders() {
  const ticker = document.querySelector(".ticker-track");
  if (ticker) {
    // Smooth infinite ticker hover pause handled via CSS, extra JS control if needed
  }
}

document.addEventListener("DOMContentLoaded", initSliders);
